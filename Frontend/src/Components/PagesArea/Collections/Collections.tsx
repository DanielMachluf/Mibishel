import { Folder } from "lucide-react";
import { ComingSoonFeature } from "../DashboardArea/ComingSoonFeature/ComingSoonFeature";

export function Collections() {
    return (
        <ComingSoonFeature
            icon={Folder}
            title="Collections"
            description="Curated boards for dinners, desserts, holidays, meal prep, and anything else you want to organize are coming soon."
        />
    );
}
